#include <stdio.h>

// Function: word_counter
// Description: This function counts the number of words in a file as well as how many of those have repeated consonants.
// Parameters:
//   file: the file to count the words in
// Return: the number of words in the file
int word_counter(const char*);